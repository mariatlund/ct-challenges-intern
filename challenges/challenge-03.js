import data from '../data/schedule.json'

/**
 * Loop through the schedule, and find all the acts that are playing.
 * The function should return a new array, containing just the name (string) of the acts.
 * - Check the `/data/schedule.json` file for structure
 * - Make sure to remove acts called `break` and acts that have been cancelled
 *
 * @param schedule
 * @returns {Array<string>}
 */
export function buildArtistList(schedule = data) {
  const artistList = []
  // Add code here ‍💻
  //

  return artistList
}

